{
    "scripts": {
        "railway:start:backend": "directus bootstrap --skipAdminInit; pnpm directus:schema:apply --yes; directus start"
      },
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "nixpacksPlan": {
        "providers": ["python"],
        "phases": {
          "setup": {
            "nixPkgs": ["...","nodePackages.pnpm"]
          }
        }
      },
      "buildCommand": "pnpm i --frozen-lockfile --force && pnpm -F frontend build"
    },
    "deploy": {
      "startCommand": "python manage.py migrate && python manage.py collectstatic --no-input && gunicorn skillsetgo.wsgi",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }
  